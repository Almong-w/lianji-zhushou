## 2026-03-04

- 调整 [`联机工具v2.js`](联机工具v2.js) 面板默认高度策略：将主面板高度改为固定值（桌面 `height:min(560px, calc(100vh - 18px))`，移动端 `height:min(500px, calc(100vh - 12px))`），使主页、房间页、设置页统一使用同一固定高度容器。
- 设置页高度改为跟随主页/房间页同一面板高度：移除 [`settings-open` 高度覆盖](联机工具v2.js)，避免切换到设置页时出现与主页不同的高度。
- 保留右下角拖拽改尺寸能力：未改动 [`startPointerResize()`](联机工具v2.js:5884) / [`onPointerResize()`](联机工具v2.js:5914) / [`applyPanelSize()`](联机工具v2.js:4592) 逻辑，继续支持手动调整宽高。

- 为 [`联机工具v2.js`](联机工具v2.js) 增加“唯一版本号”隔离机制：新增 `PLUGIN_DISPLAY_VERSION` 与 `PLUGIN_VERSION_TOKEN`，并将容器ID、样式ID、Pinia Store ID、本地存储键（设置/变量模式）、提示词注入ID、ACU回调键改为按版本派生；同时保留对旧存储键的回读兼容，避免新旧版本互相覆盖，支持并存。
- 在设置页“日志”标签中“调试日志”列表下方新增版本展示文案：`版本: <唯一版本号>`，便于直接确认当前运行插件版本。

- 修复 [`submitToAI()`](联机工具v2.js:2390) 在“立即发送”场景可能被按钮点击与自动触发并发命中导致重复推送的问题：新增 `submitInFlight` 互斥锁，确保同一轮仅执行一次“用户层创建 + 广播 user_message + 触发生成”，避免观众/玩家端出现双用户层与双角色层。

- 为 [`联机工具v2.js`](联机工具v2.js) 主面板新增右下角拖拽缩放能力：增加 `panel-resize-handle` 句柄样式与对应指针事件逻辑，支持同时调整宽高，并在视口变化时保持尺寸/位置钳制。
- 调整观众胶囊配色与普通玩家一致：将 `.user-item.spectator-item` 的背景色与边框色改为灰色体系，移除原蓝色高亮。
- 移除“隐藏模式仅注入 AI、不注入用户层”的旧链路：[`submitToAI()`](联机工具v2.js:2388) 统一将完整输入写入用户层；删除隐藏重 roll 补偿注入逻辑，隐藏模式仅影响“本轮输入池”展示为 `********`。
- 更新“隐藏模式”设置提示文案，明确开启后仅隐藏房间内输入池显示，不影响用户层真实发送内容。

## 2026-03-03

- 调整 [`联机工具v2.js`](联机工具v2.js) 玩家设置页：移除“同步选项”多选展示，将“同步历史/同步正则/同步变量/同步脚本”按钮迁移到“同步正则”区块，并改为纵向堆叠布局。
- 删除房间主界面（玩家/观众区域）内原“同步历史/同步正则/同步变量/同步脚本”按钮行，统一改由设置页入口触发。
- 新增“入房后首次点击触发同步”机制：首次用户点击即执行一次历史/正则/变量/脚本同步，并启动历史自动同步定时器；增加防重复与断开/切房清理，避免重复启动。

- 修复 [`submitToAI()`](联机工具v2.js:2366) 在“立即发送”后仅隐藏输入时会删除刚写入用户层消息的问题：去除 `deleteChatMessages(hostMsgId)` 分支，改为始终保留并回写用户层文本，避免误触发删除同步导致首条楼层被删。
- 修复 [`submitToAI()`](联机工具v2.js:2366) 的用户层注入异常：新增 `userLayerMessage` 回退占位（`[联机输入已隐藏]`），确保“立即发送”始终存在用户层输入，不再表现为仅出现角色/assistant 层回复。

- 在 [`联机工具v2.js`](联机工具v2.js) 新增“同步脚本”能力：增加 `sync_scripts_request/sync_scripts` 协议分支，基于 `getScriptTrees({ type: 'character' })` 读取角色脚本树并随包携带 `getAllEnabledScriptButtons()` 快照。
- 新增客户端脚本应用与回滚链路：接收脚本同步包后先备份本地脚本状态，断开连接（退出房间）时自动恢复本地角色脚本，保持与历史/正则/变量同步流程兼容。
- UI 新增“同步脚本”按钮（位于“同步变量”之后），并将同步按钮网格列数从 3 调整为 4；同步结果沿用现有日志与超时处理风格。
- 设置页“同步选项”新增“角色脚本”开关并接入自动同步流程；默认自动同步项包含“角色脚本”。

- 修复 [`联机工具v2.js`](联机工具v2.js) 设置页切换为 v1 同款“单页切换”：主内容区仅在 `!showSettings` 时渲染，避免“主页 + 设置页”同时存在导致的双滚动层问题。
- 删除魔法棒扩展菜单中的“联机工具设置”入口注入链路：移除入口注册、事件监听与卸载清理逻辑，扩展菜单不再出现该入口。

- 缩小态 UI 重构为悬浮球：调整主面板最小化样式（`52x52` 圆形，移动端 `48x48`），并在最小化状态隐藏标题与头部操作区，仅保留中心图标用于点击/拖拽交互。
- 头部状态展示改造为 Discord 图标：移除独立状态点（`status-dot`）渲染与样式，新增 `discord-status-icon` 状态类，通过图标本体动态着色展示连接状态。
- 在线状态颜色映射接入：正常在线（`info/debug/milestone`）显示绿色；在线异常/未知/错误显示黄色；断开状态保留灰色降级显示。
- “离开房间”按钮从缩小态/头部移入房间内部：删除头部断开按钮，在连接后展开内容区新增“离开房间”按钮，避免最小化球体中出现退出入口。
- 移除缩小态中的“联机工具”文本，仅在展开态保留标题文本，满足悬浮球形态下的纯图标展示。

- 重构 [`injectStyles()`](联机工具v1.0.js:168) 的 CSS 注入块：移除 `:root` 变量定义与全部 `var(--mp-*)` 引用，改为等价的直接视觉值内联，保持面板颜色、间距、圆角、阴影与动画观感不变。
- 合并并清理样式冗余：删除重复移动端阴影规则，收敛“重点文本字体”重复声明，继续维持原有类名与交互状态选择器兼容。
- 移动端样式保留单一 `@media` 覆盖结构：将原变量覆盖改为显式属性覆盖，减少分散定义并压缩样式体积。

- 设置入口迁移到魔法棒扩展菜单：新增 [`registerMagicWandSettingsEntry()`](联机工具v1.0.js:90) 动态注册逻辑，向扩展菜单注入“联机工具设置”入口，并在热重载/卸载时清理。
- 设置界面改为独立页面弹层：新增 [`mp-settings-page-overlay`](联机工具v1.0.js:892) 与页面容器样式，点击菜单入口通过 [`MP_OPEN_SETTINGS_EVENT`](联机工具v1.0.js:67) 直接打开独立设置页。
- 设置页右上角新增关闭按钮并移除旧入口：删除面板头部“设置”按钮与 `settings-open` 旧状态绑定，改为在独立设置页头部使用关闭按钮收起页面，原标签页与设置项逻辑保持不变。

- 设置页新增“外观”标签：在 [`SETTINGS_TAB_OPTIONS`](联机工具v1.0.js:4187) 中将 `appearance` 置于标签列表最前，新增“点击空白最小化”开关并放入外观页（样式沿用既有 toggle 交互）。
- 移除“发送用户设定”开关并改为默认发送：删除设置页对应 toggle 区块；在 [`sendInput()`](联机工具v1.0.js:4484) 与 [`sendPersonaUpdateIfNeeded()`](联机工具v1.0.js:5000) 中改为无条件按可用内容发送 `user_persona`。
- 将移动端“点击空白自动最小化”改为可配置行为：在 [`defaultSettings`](联机工具v1.0.js:4881) 新增 `autoMinimizeOnOutsideTap`，并在 [`onDocumentPointerDownAutoMinimize()`](联机工具v1.0.js:5459) 中受该开关控制。
- “点击空白最小化”从仅移动端改为桌面与移动端通用：移除 [`onDocumentPointerDownAutoMinimize()`](联机工具v1.0.js:5459) 中 `isMobileLike` 限制，并将外观页提示文案改为通用表述。

- 首页在线房间卡片移除房主显示：删除房间元信息中的 `by creatorName` 渲染，并移除 `syncCurrentRoomCreatorName` 及其 `watch` 同步链路。
- 修复“仅房主在线”时误报同步超时：在历史/正则/变量同步请求前新增可同步目标判断，无目标时直接“成功跳过”，不进入超时计时。
- 修复消息预览与用户设定预览刷新滞后：消息预览改为实时反映当前输入内容；用户设定预览改为事件驱动刷新，并在设定变更时自动广播 `user_persona` 同步给房间内他端。

- 设置页“日志”标签移除调试日志折叠交互：删除 `debugPanelCollapsed` 状态与折叠按钮/条件分支，调试日志改为始终展开显示。
- 聊天房间顶部“等待操作/等待中”状态横幅移除渲染：连接态下不再输出 `top-status-line` 提示条（含感叹号图标）。
- 聊天消息内容区增加 4 行高度上限与内部滚动：`chat-logs` 中单条消息改为固定头部 + 内容区滚动，避免超长文本继续拉高消息卡片外边框。

## 2026-03-02

- 历史同步链路新增批次号 `batchId`：在请求/响应/ACK 全链路透传（`sync_history_request`、`sync_history_data`、`sync_history_ack`），用于标识单次同步任务并支撑断点续传。
- 历史同步改为支持分片批量下发：新增 `phase=start/chunk/complete/fail` 协议与 `messages` 分片结构，默认按 `chunkSize` 批量发送；保留旧格式单条 `role/message` 兼容处理。
- 客户端历史写入增加多层去重：同批次分片去重（`seenPackets`）、按 `sourceIndex + 指纹` 去重，避免重复包与重复点击造成重复落库。
- 增强历史同步可见状态闭环：补充 `multiplayer_sync_history_progress` 事件与 UI 进度文案（开始/进行中/完成/失败），并与 `syncState.history` 统一收敛。
- 历史消息写入纳入串行队列：`multiplayer_sync_history_data` 与 `multiplayer_user_message` 统一走 `enqueueStreamTask`，减少与流式回复/变量同步的并发交错。
- 新增可用续传能力：客户端发起同步时携带 `resumeFrom`，房主记录 ACK 偏移并从已确认位置继续下发。

- 修改 [`--mp-z`](联机工具v1.0.js:85) 的层级值为 `99999`，将 v1.0 面板 UI 置顶显示，确保位于 SillyTavern 设置页面上方。
- 修复移动端键盘收起后的面板定位异常：在 [`onViewportResize()`](联机工具v1.0.js:4727) 中增加键盘收起稳定等待与高度变化判定，避免过渡帧把面板 `top` 钳制为 `0`。
- 在 [`clampPanelIntoViewport()`](联机工具v1.0.js:4689) 增加移动端过渡高度保护，并通过稳定位置缓存与恢复逻辑降低“面板被吸到顶部”概率。
- 修复 MVU 同步写入目标楼层错误：客户端不再直接使用 [`getLastMessageId()`](联机工具v1.0.js:2923) 作为变量落点，而是通过 [`waitForAssistantMessageId()`](联机工具v1.0.js:2948) 与 [`findLatestAssistantMessageId()`](联机工具v1.0.js:2922) 定位最近助手消息。
- 修复 MVU 与 AI 回复并发竞态：将 [`multiplayer_sync_variables`](联机工具v1.0.js:3751) 处理并入 [`enqueueStreamTask()`](联机工具v1.0.js:2890) 串行队列，避免变量先于助手消息落地导致错位。
- 增强 MVU 同步链路的可判定性：房主同步包新增 [`_syncMeta`](联机工具v1.0.js:3202)，并在“无可写助手消息”时返回失败而非误报成功。
- 修复“在线房间仅房主一人”时房主身份未及时建立导致的历史/正则/变量同步超时：在 [`postJoinSync()`](联机工具v1.0.js:2303) 增加 `assumeHost` 逻辑，创建者入房后立即具备 host 身份并主动广播用户状态。
- 调整在线建房后入房流程：在 [`createAndJoinRoom()`](联机工具v1.0.js:4076) 调用 [`connectOnline()`](联机工具v1.0.js:2344) 时传入 `{ assumeHost: true }`，避免等待外部回包才能识别房主。
- 优化创建者同步行为：当本端已按创建者身份判定为 host 时，不再发送 [`request_pending_inputs`](联机工具v1.0.js:2340) 自请求，减少单人房无效同步链路。
- 将 v1.0 的“同步历史”流程回退为 v0.7 逻辑：[`requestSyncHistory()`](联机工具v1.0.js:4199) 改为仅发送 `sync_history_request` 并记录请求日志，不再依赖 [`startSync()`](联机工具v1.0.js:3922)/[`finishSync()`](联机工具v1.0.js:3931) 结果链路。
- 调整历史同步调用链对齐 v0.7：在消息入口 [`case 'sync_history_request'`](联机工具v1.0.js:2055) 增加房主侧请求日志；在 [`multiplayer_sync_history_request`](联机工具v1.0.js:3423)/[`multiplayer_sync_history_data`](联机工具v1.0.js:3484) 移除 `sourceIndex` 去重更新分支，恢复为顺序下发与逐条创建消息。
- 移除离线房间与离线连接能力：删除本地通道客户端与离线连接入口，仅保留在线 WebSocket 流程（[`initNetwork()`](联机工具v1.0.js:1742)、[`connectOnline()`](联机工具v1.0.js:2244)）。
- 清理离线相关状态与设置项：删除 `onlineMode`/离线端口密码等状态分支，房间拉取与设置页改为纯在线逻辑（[`settings`](联机工具v1.0.js:1296)、[`fetchRooms()`](联机工具v1.0.js:3792)、[`defaultSettings`](联机工具v1.0.js:4262)）。
- 清理在线模式按钮中的离线入口：断开连接界面仅保留在线房间列表与创建加入入口，设置中移除“在线模式”切换，仅保留服务器地址配置（[`MultiplayerPanel.render`](联机工具v1.0.js:4957)、[`服务器地址`](联机工具v1.0.js:5323)）。

- 对 v1.0 的 MVU 同步机制实施 MVP 改造：[`waitForAssistantMessageId()`](联机工具v1.0.js:2845) 在定位失败时不再回退到最后助手楼层，统一返回失败由上层中止写入。
- 新增 `sync_variables` 包幂等去重：发送端附加 [`packetId`](联机工具v1.0.js:3203)，接收端维护最近 50 条去重缓存并对重复包返回“已忽略”成功结果（[`rememberSyncPacketId()`](联机工具v1.0.js:2794)）。
- 扩大 MVU 自动同步判定范围：以 [`hasMvuSyncPayload()`](联机工具v1.0.js:2809) 统一覆盖 `stat_data/display_data/delta_data/schema` 任一字段即同步。
- 移除固定 200ms 延迟写法：AI 生成结束后改为小窗口轮询等待变量就绪（[`waitForMvuPayloadReady()`](联机工具v1.0.js:2870)），超时仅记录日志不发送空包。
- 修正变量类型返回语义：[`multiplayer_sync_variables`](联机工具v1.0.js:3953) 对非 `mvu` 的未知 `variableType` 返回 `ok:false` 与“未处理类型”信息，避免假成功。

- 重构并精简 [`联机工具v1.0.js`](联机工具v1.0.js)：将单一范围正则同步的别名/归一化兼容层直接内联为 `character` 主路径，删除对应冗余适配逻辑。
- 清理历史同步会话中的无效状态字段：移除未参与流程判断的 `updatedAt` 与 `done`，保留 `seenPackets/seenLegacyItems` 去重核心路径。
- 删除未使用的 ACU 注册守卫常量，仅保留实际使用的回调句柄注册/卸载链路，减少重复状态维护。
- 删除仅用于临时排障的系统日志噪声（历史请求入口与房主历史发送准备日志、MVU 自动同步跳过日志），保持既有同步行为不变。

- 聊天日志展示改为分层路由：系统 `milestone/error` 保留在聊天区，系统 `info/debug` 改为仅更新顶部单行状态并进入调试面板，减少聊天区刷屏。
- 删除原 `transientSystemLogs + TTL` 的实时消失日志链路与合并逻辑，简化为按级别单入口分发。
- 新增顶部单行状态区（仅显示最新一条）与默认折叠的调试面板；调试日志限制最近 `30` 条，超出自动丢弃。
- 删除 v1.0 主题导入子系统：移除 `onThemeFileChange()` 及其主题 token 解析/应用链路，清理设置页「UI主题导入」入口与文件选择控件，仅保留基础 `--mp-*` CSS 变量样式体系。

- UI 结构调整：将原主面板中的“调试日志”独立区块移除，避免与设置内日志重复入口（[`联机工具v1.0.js`](联机工具v1.0.js:5702)）。
- 设置页签新增“日志”：在设置 Tab 列表中追加 `logs` 选项，形成“通用/房主/玩家/日志”结构（[`SETTINGS_TAB_OPTIONS`](联机工具v1.0.js:4167)）。
- 日志功能迁移：将原调试日志渲染与折叠交互迁移到设置页“日志”标签内，继续复用同一数据源与样式（[`store.currentDebugLogs`](联机工具v1.0.js:5828)、[`debugPanelCollapsed`](联机工具v1.0.js:5824)、[`debug-logs`](联机工具v1.0.js:5827)）。
